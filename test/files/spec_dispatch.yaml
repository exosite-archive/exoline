dataports:
    - alias: temp 
      format: float
    - alias: humidity
      format: float

dispatches:
    - alias: notify
      subscribe: temp
      method: email
      recipient: danweaver+exolinetest@exosite.com
      subject: hello from Exoline test
      message: hi
