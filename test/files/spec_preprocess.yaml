# Test spec
dataports:
    - alias: variable
      format: float
    - alias: math
      format: float
      preprocess:
      - [add, 4.4]
      - [mul, 2]
    - alias: complex
      format: float
      preprocess:
      - [add, variable]
      - [mul, 2]

